{
  "name": "cloudflare-worker-router-typescript-example",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run check && npm run compile",
    "compile": "node ./scripts/build.js",
    "check": "npm run clean && tsc -p tsconfig.build.json",
    "clean": "rm -rf dist/*"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cloudflare-worker-router-typescript": "file:.."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.4.0",
    "@types/glob": "^7.2.0",
    "@types/node": "^17.0.21",
    "esbuild": "^0.14.25",
    "glob": "^7.2.0",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  }
}
